产品名称：Vosk 语音输入助手（Java 版）

一、产品概述

一个基于 Java + Vosk 离线语音识别 的桌面工具。用户长按自定义热键说话，松开后语音自动转为文字，并模拟键盘输入到当前焦点窗口。

核心价值：无需联网，隐私安全，一键语音转文字，提升输入效率。



产品名称

(1)中文名:Vosk语音输入

(2)英文名:VoskType

二、核心功能模块

1\. 全局热键触发录音

(1)触发方式：长按用户自定义的热键（默认V）。



按下：开始录音，给出视觉/听觉反馈。



松开：停止录音，进入识别流程。



(2)触发方式2:点击用户自定义的热键(默认V)

点击后开始录音并给出视觉/听觉反馈,同时自动进入识别流程,再此点击热键关闭录音



防误触：可设置最短录音时长（例如 0.3 秒），短于此时间的录音被忽略。



2\. 录音模块

音频参数：16kHz 采样率、16bit 位深、单声道、PCM 格式。



录音设备：使用系统默认麦克风，支持后续在设置中切换设备。



状态指示：



系统托盘图标变为“录音中”样式。



可选的悬浮小窗显示“正在聆听...”。



如果启用声音提示，录音开始/结束各播放一个短促音效。



异常处理：无麦克风权限或设备占用时，弹窗提醒或托盘图标闪烁。



3\. 语音识别模块（Vosk 离线引擎）

模型管理：



指定 Vosk 模型文件夹路径（如 model/vosk-model-small-cn-0.22）。



应用启动时加载模型，若路径无效则提示并退出。



支持运行时动态切换模型（中文小模型/大模型/英文模型）。



识别流程：



录音结束后，将完整 PCM 数据传入 Vosk 识别器。



获取完整的识别文本（可带标点，取决于模型）。



可选流式识别：边录边识，松开时直接返回已识别的中间结果，减少等待感。



识别超时：若 5 秒内未返回结果，视为识别失败，提示用户。



4\. 文字输入模块

输入方式：模拟键盘输入到当前活动窗口。



中文输入：优先采用模拟粘贴（Ctrl+V）方式，先复制文字到剪贴板，再发送粘贴快捷键。操作前备份剪贴板内容，粘贴后恢复，避免干扰用户。



英文/数字：可直接用 Robot 模拟打字，效率更高。



自动后处理：



去除首尾多余空格。



可选在末尾自动添加空格、换行、标点（配置项）。



可选“自动发送消息”模式(可在设置中开启,默认关闭)：识别文本末尾自动补一个回车键（适用于聊天框）。



5\. 系统托盘与常驻后台

托盘图标：显示三种状态：



待机（默认图标）



录音中（动态或红色圆点）



识别中（沙漏或转圈）



出错（感叹号）



右键菜单：



开始/停止监听（临时禁用热键）



设置（打开配置界面）



切换模型（中文/英文/自定义）



查看本次识别历史



退出程序



左键单击：快捷切换“是否启用语音助手”。



6\. 配置与设置界面（可选扩展）

用 JavaFX 或 Swing 实现一个简单的设置窗口：



热键设置：录制新快捷键（支持组合键）。



模型路径：浏览选择文件夹。



语音检测灵敏度：VAD 开启/关闭，静音阈值。



输入模式：粘贴模式 / 模拟打字模式。



标点与后缀：自动添加句号、换行、空格等。



声音提示：开关，自定义音效文件。



启动时最小化、开机自启。



三、详细功能交互流程

text

用户长按热键

&#x20; → 系统检测到热键按下（排除重复触发）

&#x20; → 检查麦克风权限

&#x20; → 开始录音，记录录音开始时间戳

&#x20; → 托盘图标变为录音中，可选悬浮窗弹出

&#x20; → 用户松开热键

&#x20;     → 检查录音时长 < 最短时长?  → 是：丢弃录音，恢复待机

&#x20;     → 停止录音，保存 PCM 数据

&#x20;     → 托盘图标变为识别中

&#x20;     → 将音频数据传给 Vosk 识别器

&#x20;     → 获得识别文本

&#x20;     → 识别成功？

&#x20;         → 是：执行输入（粘贴/打字）

&#x20;         → 否：提示“未识别到语音”，托盘显示错误状态 2 秒

&#x20;     → 恢复待机图标

四、技术实现注意事项（供 Trae 参考）

音频采集：使用 javax.sound.sampled.TargetDataLine，格式指定为 AudioFormat(16000, 16, 1, true, false)。



全局热键：采用 com.github.kwhat.jnativehook 库，监听全局键盘按下/释放。按下和释放成对判定，防止长按重复触发。



Vosk 集成：使用官方 Java 绑定，vosk 依赖，LibVosk.vosk\_set\_log\_level(-1) 关闭日志。实例化 Model 和 Recognizer，调用 recognizer.acceptWaveForm(bytes) 或 recognizer.getResult()。



剪贴板操作：使用 java.awt.Toolkit 和 Transferable，备份与恢复需同步处理，防止并发冲突。



托盘：使用 java.awt.SystemTray + PopupMenu，图标资源可准备多张 PNG。



打包：使用 jlink + jpackage 生成原生安装包，将 Vosk 本地库（.dll/.so/.dylib）和模型文件夹一并打包。



五、拓展功能（后续版本可选）

拓展功能	描述

命令模式	识别到特定关键词（如“换行”、“删除”、“回车”）时，不输出文字，而是执行相应键盘操作。

多语言实时切换	热键 Ctrl+Alt+L 在中/英文模型间切换，无需进入设置。

热词替换	自定义词典，将口语词自动替换为书面语（如“扣扣”→“QQ”）。

录音历史	保存最近 N 条录音和识别结果，支持点击重播或重新输入。

流式上屏	边说话边识别，实时打字到屏幕（类似讯飞输入法流式模式）。

插件式输出	支持将识别文本发送到 HTTP 接口、WebSocket 或剪贴板（而不只是键盘输入）。

多平台适配	Windows/macOS/Linux 系统差异处理（如 macOS 辅助权限提示）。

便携版模式	所有配置、模型路径使用相对路径，可放在 U 盘直接运行。

六、非功能性需求

性能：工具常驻内存占用 < 150 MB（含小模型），识别延迟 < 500ms（从录音结束到文字出现）。



稳定性：连续 24 小时运行不崩溃，热键监听不丢失。



兼容性：支持 Windows 10/11、macOS 12+、主流 Linux 发行版。



易用性：首次运行自动检查模型是否存在，若不存在给出下载引导；托盘菜单全中文。

